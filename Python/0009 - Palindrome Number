class Solution:
    def isPalindrome(self, x: int) -> bool:
        stringNum = str(x)
        new = stringNum[::-1]
        if stringNum == new:
            return True
        else:
            return False
