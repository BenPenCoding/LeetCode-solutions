class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        array = []
        for i in range(len(nums)):
            num = target - nums[i]
            print(num)
            if nums[i] in array:
                return [i,nums.index(num)]
            else:
                array.append(num)
